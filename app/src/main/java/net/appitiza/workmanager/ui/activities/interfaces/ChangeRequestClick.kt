package net.appitiza.workmanager.ui.activities.interfaces

import net.appitiza.workmanager.model.ChangeDeviceRequestData

interface ChangeRequestClick {
    fun onClick(data: ChangeDeviceRequestData)
}